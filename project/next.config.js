/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // PWA configuration will be added when next-pwa is configured
  // For now, we'll use manifest.json for PWA support
};

module.exports = nextConfig;
